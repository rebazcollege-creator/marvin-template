# reference parts library — read-only, never imported, never committed
/reference

# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# tauri
/src-tauri/target
/src-tauri/gen
# compiled sidecar binary (built by `npm run sidecar:build`)
/src-tauri/binaries

# env — never commit secrets
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.idea
.vscode
*.tsbuildinfo
next-env.d.ts

# Dev-only sidecar credential store (entered via Connections)
.xani-creds.json

# Voice corpus + analysis (Rebaz's own harvested writing — private, never in git)
.xani-voice-corpus.json
.xani-voice-analysis.json
