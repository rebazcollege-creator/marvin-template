{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main Xanî window. Plugin permissions (keyring, sql, etc.) are added here as those plugins are wired in later phases.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
