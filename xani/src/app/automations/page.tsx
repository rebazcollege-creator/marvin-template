export default function AutomationsPage() {
  return (
    <div className="mx-auto max-w-3xl px-8 py-10">
      <h1 className="font-display text-2xl text-text">Automations</h1>
      <p className="mt-1 text-sm text-muted">Standing instructions Xanî runs for you — on a schedule, or the moment something happens</p>
      <div className="mt-8 rounded-2xl border border-dashed border-border bg-surface p-8 text-sm text-text-2">
        Describe what Xanî should do, pick a template, and it runs in the background. The full builder is being built.
      </div>
    </div>
  );
}
