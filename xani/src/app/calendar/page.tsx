import { PagePlaceholder } from '@/components/ui/PagePlaceholder';

export default function CalendarPage() {
  return (
    <PagePlaceholder
      title="Calendar"
      subtitle="Multi-calendar · Europe/Berlin"
      note="Pending Google Calendar OAuth (Phase 4). Today's events across all calendars will appear here. Creating or changing events always requires confirmation."
    />
  );
}
