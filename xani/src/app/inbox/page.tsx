import { PagePlaceholder } from '@/components/ui/PagePlaceholder';

export default function InboxPage() {
  return (
    <PagePlaceholder
      title="Inbox"
      subtitle="Unified Gmail · 5 accounts"
      note="Pending Gmail OAuth (Phase 4). Once each account is authorised, unread messages appear here, labelled by role: Personal, Moonshot, LeadStories, Zoho, Amargi."
    />
  );
}
