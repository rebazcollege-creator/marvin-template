import { PagePlaceholder } from '@/components/ui/PagePlaceholder';

export default function SlackPage() {
  return (
    <PagePlaceholder
      title="Slack"
      subtitle="2 workspaces · Amargi + LeadStories"
      note="Amargi wired via direct MCP (#general C0HRYE891, #tt-arabic C052Z75EY73). LeadStories pending OAuth and is monitor-only — MARVIN alerts you to emergency trend drops but never posts there."
    />
  );
}
