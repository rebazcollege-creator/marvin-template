import { PagePlaceholder } from '@/components/ui/PagePlaceholder';

export default function EmailStudioPage() {
  return (
    <PagePlaceholder
      title="Email Drafter"
      subtitle="Studio · multi-account"
      note="Give intent, recipient and which Gmail account; MARVIN drafts the email. Sending always requires confirmation — LeadStories requires explicit per-message confirmation and is never automated. Editor UI lands in Phase 3."
    />
  );
}
